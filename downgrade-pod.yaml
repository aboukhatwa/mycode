apiVersion: v1
kind: Pod
metadata:
  name: nginx-obj-create
spec:
  #selector: 
  #  matchLabels:
  #     app: nginx
  # replicas: 3
  # template:
  #  metadata:
  #    labels:
  #      app: nginx
  #  spec:
      containers:
      - name: nginx
        image: nginx:1.7.9
        ports:
        - containerPort: 80
